BoxLayout:
    orientation: 'vertical'
    Label:
        text:'Testing time yeah'
    MyBoxLayout:
        #These buttons will apperar vertical even though we're not defining it in .kv (because we did in the class definition)
        Button:
            text:'Liverpool Football Club'
        Button:
            text: 'uovo'